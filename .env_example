# =========================
# Database (SQL Server)
# =========================
DB_SERVER=.
DB_NAME=PharmacyDB
DB_DRIVER={ODBC Driver 17 for SQL Server}

# If you use Windows Trusted Connection keep DB_TRUSTED=true and leave DB_USERNAME/DB_PASSWORD empty.
# If you use SQL Authentication set DB_TRUSTED=false and provide DB_USERNAME/DB_PASSWORD.
DB_TRUSTED=true
DB_USERNAME=
DB_PASSWORD=

# =========================
# Ollama LLM
# =========================
# Local model name you've pulled with `ollama pull`
OLLAMA_MODEL=qwen2.5-coder:7b
# Optional: if Ollama runs on a different host/port, set base URL (default: http://127.0.0.1:11434)
OLLAMA_BASE_URL=
OLLAMA_TEMPERATURE=0.0
OLLAMA_NUM_PREDICT=256

# =========================
# API & Security
# =========================
# Comma-separated list of origins for CORS. Use "*" for all (dev only).
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Max rows returned in preview (server trims larger results)
PREVIEW_LIMIT=200
